%h3 Daily Order
%table.table.table-bordered
  %tr
    %th Terminal
    %th Menu Item
    %th Quantity
  - @daily_orders.each do |order|
    %tr
      %td= order.terminal_name
      %td= order.menu_item_name
      %td= order.total_quantity